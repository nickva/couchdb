diff --git a/src/couch/priv/couch_js/quickjs/quickjs/quickjs.c b/src/couch/priv/couch_js/quickjs/quickjs/quickjs.c
index 791601391..81e68b36d 100644
--- a/src/couch/priv/couch_js/quickjs/quickjs/quickjs.c
+++ b/src/couch/priv/couch_js/quickjs/quickjs/quickjs.c
@@ -28692,10 +28692,24 @@ static __exception int js_parse_source_element(JSParseState *s)
     if (s->token.val == TOK_FUNCTION ||
         (token_is_pseudo_keyword(s, JS_ATOM_async) &&
          peek_token(s, TRUE) == TOK_FUNCTION)) {
+
+        if (peek_token(s, TRUE) == '(') {
+           /* Spidermonkey 1.8.5 mode: accept top function statements as expressions */
+           if (js_parse_expr(s))
+               return -1;
+           if (s->cur_func->eval_ret_idx >= 0) {
+               /* store the expression value so that it can be returned by eval() */
+               emit_op(s, OP_put_loc);
+               emit_u16(s, s->cur_func->eval_ret_idx);
+           } else {
+               emit_op(s, OP_drop); /* drop the result */
+           }
+        } else {
         if (js_parse_function_decl(s, JS_PARSE_FUNC_STATEMENT,
                                    JS_FUNC_NORMAL, JS_ATOM_NULL,
                                    s->token.ptr, s->token.line_num))
             return -1;
+        }
     } else if (s->token.val == TOK_EXPORT && fd->module) {
         if (js_parse_export(s))
             return -1;
